
describe('Writing a spec', () => {

    it('it is easy when you know how!', () => {

        // Given (arrange)
        const a = 10, b = 20;

        // When (act)
        const answer = a + b;

        // Then (assert)
        expect(answer).toBe(30);

    });
});